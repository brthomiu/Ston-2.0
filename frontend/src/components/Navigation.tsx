import LoginButton from './LoginButton';
import LogoutButton from './LogoutButton';

function Navigation() {
  return (
    <nav>
      <h1>Navbar!</h1>
      <LoginButton />
      <LogoutButton />
    </nav>
  );
}

export default Navigation;
