// import { useAuth0 } from '@auth0/auth0-react';
// import { deleteAccount } from '../features/authService';

// function DeleteAccountButton() {
//   const { user, isAuthenticated } = useAuth0();

//   return (
//     isAuthenticated &&
//     user && (
//       <button type="button" onClick={() => deleteAccount(user)}>
//         Delete Profile
//       </button>
//     )
//   );
// }

// export default DeleteAccountButton;
